# Variables

A variable is a symbol that represents an unknown value.

Example:

x = 5

If:

x + 3 = 8

Then:

x = 5

Programming also uses variables, although they store information instead of unknown values.
